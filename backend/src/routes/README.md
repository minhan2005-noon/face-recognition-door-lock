# Routes

Routes định nghĩa các endpoint REST API.

## Cần làm

- `/api/users`: quản lý người dùng.
- `/api/access-logs`: xem lịch sử ra vào.
- `/api/lock`: gửi lệnh khóa/mở khóa.
- Gắn middleware xác thực cho endpoint nhạy cảm.

