# Mã nguồn Backend

Thư mục mã nguồn chính của backend.

## Cần làm

- `app.js`: khởi tạo Express app và middleware chung.
- `routes/`: định nghĩa endpoint.
- `controllers/`: xử lý request/response.
- `services/`: logic nghiệp vụ.
- `middleware/`: xác thực, logging, error handler.

