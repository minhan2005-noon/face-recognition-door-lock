# Web

Thư mục web chứa các ứng dụng web bổ sung cho dự án.

## Cần làm

- Đặt app chính trong `app/`.
- Có thể tách dashboard riêng nếu cần phân quyền admin/user.
- Đồng bộ API endpoint với backend.

## Cài đặt

- Cài Node.js LTS.
- Mỗi app nên có `package.json` riêng.

